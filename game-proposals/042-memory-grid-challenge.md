# 042 - Memory Grid Challenge (기억력 격자 도전)

## 게임 컨셉
N×N 격자에 배치된 카드를 뒤집어 같은 쌍을 찾는 클래식 메모리 매칭 퍼즐. 2026 두뇌 훈련 트렌드(Brain School itch.io 인기) + 하이브리드 캐주얼 접근(프로그레션 시스템). 단순한 매칭을 넘어 콤보 체인, 특수 카드, 일일 두뇌 점수까지 제공.

## 핵심 메카닉
- **카드 뒤집기**: 탭으로 카드 뒤집기, 2장씩 매칭
- **매칭 시스템**: 같은 그림 2장 → 매치 성공 → 사라짐
- **콤보 체인**: 연속 매칭 시 점수 배율 증가 (×2, ×3... ×5)
- **특수 카드** (레벨 진행 시 등장):
  - ⚡ 번개: 매칭 시 주변 1칸 모두 공개 (3초)
  - 🔮 수정구: 모든 카드 1초간 전체 공개
  - 💣 폭탄: 매칭 실패 시 폭발 → 라이프 -1
  - 🌀 셔플: 매칭 시 남은 카드 위치 섞임
- **시간 제한**: 레벨별 타이머 (여유 → 타이트)
- **턴 제한**: 상위 레벨에서 뒤집기 횟수 제한 추가

## 차별점
1. **포트폴리오 미보유**: 메모리/카드 매칭 장르 전무
2. **두뇌 훈련 트렌드**: itch.io Brain School, 관찰력/기억력 게임 인기
3. **특수 카드 전략성**: 단순 매칭을 넘어 전략적 판단 추가
4. **일일 두뇌 점수**: 리텐션 킬러 — 매일 점수 기록, 추세 그래프
5. **하이브리드 캐주얼**: 프로그레션 + 수집 요소로 장기 리텐션

## 타겟 유저
- **1차**: 전연령 캐주얼 (규칙 직관적)
- **2차**: 두뇌 훈련 선호층 (기억력 향상 목적)
- **3차**: 경쟁형 유저 (리더보드, 일일 랭킹)

## 수익 모델
- **보상 광고**: 추가 시간 (+10초), 힌트 (1쌍 공개)
- **전면 광고**: 5라운드마다
- **IAP**: 광고 제거 $2.99, 테마 팩 $0.99 (이모지/동물/국기 등)
- **데일리 챌린지**: 고정 시드, 리텐션 극대화

## 기술 스택
- Pure HTML5 Canvas (단일 파일)
- Vanilla JavaScript
- CSS 애니메이션 (카드 뒤집기 3D 효과)
- LocalStorage (점수, 두뇌 점수 추세, 통계)
- PWA 지원

---

## 상세 설계

### 시스템 설계

#### 카드 생성 시스템
```javascript
const THEMES = {
  emoji: ['🐶','🐱','🐭','🐹','🐰','🦊','🐻','🐼','🐻‍❄️','🐨',
          '🐯','🦁','🐮','🐷','🐸','🐵','🐔','🐧','🐦','🦅'],
  shapes: ['circle','square','triangle','diamond','star','hexagon',
           'heart','moon','sun','cloud'],
  colors: ['#FF6B6B','#4ECDC4','#FFE66D','#95E1D3','#F38181',
           '#AA96DA','#6C5CE7','#00B894','#FDCB6E','#E17055']
};

function generateLevel(level) {
  const gridConfig = getGridConfig(level);
  const pairCount = (gridConfig.cols * gridConfig.rows) / 2;
  const theme = THEMES[getCurrentTheme()];
  
  // 쌍 생성
  const symbols = shuffleArray(theme).slice(0, pairCount);
  const cards = [...symbols, ...symbols]; // 쌍으로 복제
  
  // 특수 카드 삽입 (레벨 20+)
  if (level >= 20) {
    insertSpecialCards(cards, level);
  }
  
  return {
    cards: shuffleArray(cards),
    gridConfig,
    timeLimit: getTimeLimit(level),
    turnLimit: level >= 40 ? getTurnLimit(level) : null
  };
}
```

#### 격자 설정
```javascript
function getGridConfig(level) {
  if (level <= 5)  return { rows: 3, cols: 4 };  // 12 = 6쌍
  if (level <= 15) return { rows: 4, cols: 4 };  // 16 = 8쌍
  if (level <= 30) return { rows: 4, cols: 5 };  // 20 = 10쌍
  if (level <= 50) return { rows: 5, cols: 6 };  // 30 = 15쌍
  if (level <= 70) return { rows: 6, cols: 6 };  // 36 = 18쌍
  return { rows: 6, cols: 7 };                    // 42 = 21쌍
}
```

#### 점수 시스템
```javascript
{
  matchScore: 100,
  timeBonus: Math.floor(remainingTime * 5),
  comboMultiplier: Math.min(consecutiveMatches, 5),
  turnBonus: turnsLeft ? turnsLeft * 20 : 0,
  totalScore: (matchScore + timeBonus + turnBonus) * comboMultiplier,
  
  // 두뇌 점수 (일일 종합)
  brainScore: {
    memory: accuracyRate * 40,       // 정확도 40%
    speed: avgMatchTime * 30,        // 속도 30%
    streak: maxCombo * 30            // 연속 매칭 30%
  }
}
```

### UI/UX 플로우

#### 메인 화면
```
┌──────────────────────┐
│  🧠 MEMORY GRID      │
│                      │
│  Brain Score: 87/100 │
│  📈 +3 from yesterday│
│  Level: 15           │
│                      │
│     [▶ PLAY]         │
│   [📅 Daily]         │
│   [📊 Stats]         │
│   [🏆 Ranks]         │
│                      │
└──────────────────────┘
```

#### 게임 화면
```
┌──────────────────────┐
│ ⏱️ 23.4s  🔥×2 Combo │
│ Pairs: 3/8   Lv.15  │
├──────────────────────┤
│                      │
│  ┌──┐┌──┐┌──┐┌──┐   │
│  │🐶││  ││  ││  │   │
│  └──┘└──┘└──┘└──┘   │
│  ┌──┐┌──┐┌──┐┌──┐   │
│  │  ││🐱││  ││🐱│   │ ← 매칭!
│  └──┘└──┘└──┘└──┘   │
│  ┌──┐┌──┐┌──┐┌──┐   │
│  │  ││  ││  ││  │   │
│  └──┘└──┘└──┘└──┘   │
│  ┌──┐┌──┐┌──┐┌──┐   │
│  │  ││  ││  ││  │   │
│  └──┘└──┘└──┘└──┘   │
│                      │
└──────────────────────┘
```

#### 두뇌 점수 화면
```
┌──────────────────────┐
│  🧠 Brain Score      │
│                      │
│     TODAY: 87        │
│                      │
│  Memory   ████████░░ 82│
│  Speed    █████████░ 89│
│  Streak   █████████░ 91│
│                      │
│  📈 7-Day Trend      │
│  M T W T F S S       │
│  72 75 78 80 83 85 87│
│  ▁ ▂ ▃ ▄ ▅ ▆ ▇      │
│                      │
│    [Share 📤]         │
└──────────────────────┘
```

### 밸런스 파라미터

| 레벨 범위 | 격자 | 시간 | 턴 제한 | 특수카드 |
|-----------|------|------|---------|---------|
| 1-5       | 3×4  | 60s  | 없음    | 없음    |
| 6-15      | 4×4  | 50s  | 없음    | 없음    |
| 16-30     | 4×5  | 45s  | 없음    | ⚡🔮    |
| 31-50     | 5×6  | 40s  | 40턴    | ⚡🔮💣  |
| 51-70     | 6×6  | 35s  | 35턴    | 전체    |
| 71+       | 6×7  | 30s  | 30턴    | 전체    |

| 파라미터 | 값 | 설명 |
|---------|-----|------|
| INITIAL_LIVES | 3 | 초기 라이프 |
| COMBO_MAX | 5 | 최대 콤보 배율 |
| PEEK_TIME | 1.5s | 카드 공개 시간 |
| MISMATCH_DELAY | 0.8s | 미스매치 후 뒤집기 딜레이 |
| AD_INTERVAL | 5 | 전면 광고 간격 |
| SPECIAL_CARD_RATIO | 0.1 | 특수 카드 비율 (Lv20+) |

---
*생성일: 2026-01-31*
*기반 트렌드: itch.io Brain School (두뇌 훈련), 하이브리드 캐주얼 프로그레션 시스템, 매치3 D30 리텐션 26%*
