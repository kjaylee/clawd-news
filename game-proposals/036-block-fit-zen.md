# 036. Block Fit Zen - 블록 핏 젠

## 게임 컨셉
**한 줄 요약:** 다양한 모양의 블록을 10×10 그리드에 배치하여 줄을 완성하는 젠 퍼즐

Woodoku (1억+ DL), Block Puzzle (수억 DL) 장르를 벤치마크. 테트리스 블록 모양의 피스를 드래그하여 그리드에 배치하고, 가로/세로 줄을 완성하면 클리어. 시간 제한 없이 차분하게 플레이하는 젠 스타일. 단순하지만 전략적 — 배치 순서와 공간 활용이 핵심.

## 핵심 메카닉
1. **블록 배치**: 3개의 블록 피스 중 하나를 그리드에 드래그하여 배치
2. **줄 클리어**: 가로/세로 줄이 완성되면 해당 줄 전체 클리어 + 점수
3. **연쇄 보너스**: 한 번에 여러 줄 클리어 시 콤보 보너스
4. **게임 오버**: 3개의 블록 중 어느 것도 배치할 수 없으면 종료
5. **하이스코어 도전**: 끝없이 이어지는 점수 도전 모드

## 블록 종류 (Pentomino 기반)
```
▪          ▪▪        ▪▪▪       ▪          ▪▪
           ▪▪                  ▪▪         ▪
                               ▪          ▪

▪▪▪▪       ▪▪▪       ▪▪        ▪▪▪        ▪
            ▪          ▪▪       ▪         ▪▪▪

▪▪▪         ▪         ▪▪▪       ▪           ▪▪▪
▪▪▪         ▪▪▪       ▪▪▪       ▪▪▪         ▪
                                 ▪
```

- 1칸 (1×1)
- 2칸 (1×2, 2×1)
- 3칸 (L, I, T 변형)
- 4칸 (테트리스 피스 7종)
- 5칸 (Pentomino 확장)
- 9칸 (3×3 정사각형) - 고난도 보너스

## 타겟 유저
- 20-55세 캐주얼 퍼즐 유저
- "두뇌 운동" 목적 플레이어
- 짧은 세션 (5-15분) 반복 유저
- 테트리스 세대 (향수 자극)
- 잠자기 전, 이동 중 가볍게 즐기는 유저

## 차별점
1. **깔끔한 우드/젠 테마**: 나무 질감 블록 + 미니멀 배경 (차분한 분위기)
2. **콤보 시스템**: 연쇄 클리어 시 화려한 이펙트 + 점수 멀티플라이어
3. **일일 챌린지**: 매일 정해진 블록 세트로 최고 점수 도전 (리텐션)
4. **3가지 모드**: 클래식(무한) / 타임어택(2분) / 퍼즐(정해진 피스로 완성)
5. **테마 스킨**: 우드, 네온, 파스텔, 밤하늘 등
6. **통계 대시보드**: 최고점수, 평균점수, 연속 플레이 일수

## 수익 모델
- **보상형 광고**: 추가 블록 1개 받기 (게임 오버 직전)
- **삽입 광고**: 게임 오버 후 (3게임마다)
- **인앱 구매**:
  - 광고 제거: $2.99
  - 테마 팩: $0.99 각
  - 프리미엄 번들 (모든 테마 + 광고 제거): $4.99
- **일일 리워드**: 연속 접속 보너스 (테마 조각 등)

## 기술 스택
- **엔진**: HTML5 Canvas + Vanilla JavaScript
- **렌더링**: Canvas 2D (10×10 그리드 + 블록 드래그)
- **애니메이션**: requestAnimationFrame (줄 클리어 파티클)
- **입력**: Touch/Mouse 드래그 앤 드롭
- **저장**: localStorage (하이스코어, 설정, 통계)
- **배포**: GitHub Pages → 텔레그램 Mini App

---

## 시스템 설계

### 그리드 데이터 구조
```javascript
const GRID_SIZE = 10;

const gameState = {
  grid: Array(GRID_SIZE).fill(null).map(() => Array(GRID_SIZE).fill(0)),
  // 0 = empty, 1+ = filled (color index)
  score: 0,
  combo: 0,
  highScore: 0,
  currentPieces: [], // 3개의 배치 가능한 피스
  stats: {
    gamesPlayed: 0,
    totalLinesCleared: 0,
    bestCombo: 0,
    longestStreak: 0,
  }
};
```

### 블록 피스 정의
```javascript
const PIECES = [
  // 1칸
  { shape: [[1]], color: 1, name: 'dot' },
  
  // 2칸
  { shape: [[1,1]], color: 2, name: 'h2' },
  { shape: [[1],[1]], color: 2, name: 'v2' },
  
  // 3칸
  { shape: [[1,1,1]], color: 3, name: 'h3' },
  { shape: [[1],[1],[1]], color: 3, name: 'v3' },
  { shape: [[1,1],[1,0]], color: 3, name: 'L3' },
  { shape: [[1,1],[0,1]], color: 3, name: 'J3' },
  
  // 4칸 (테트리스)
  { shape: [[1,1,1,1]], color: 4, name: 'I4' },
  { shape: [[1],[1],[1],[1]], color: 4, name: 'I4v' },
  { shape: [[1,1],[1,1]], color: 4, name: 'O4' },
  { shape: [[1,1,0],[0,1,1]], color: 4, name: 'S4' },
  { shape: [[0,1,1],[1,1,0]], color: 4, name: 'Z4' },
  { shape: [[1,1,1],[1,0,0]], color: 4, name: 'L4' },
  { shape: [[1,1,1],[0,0,1]], color: 4, name: 'J4' },
  { shape: [[1,1,1],[0,1,0]], color: 4, name: 'T4' },
  
  // 5칸 (일부)
  { shape: [[1,1,1,1,1]], color: 5, name: 'I5' },
  { shape: [[1],[1],[1],[1],[1]], color: 5, name: 'I5v' },
  { shape: [[1,1,1],[1,0,0],[1,0,0]], color: 5, name: 'bigL' },
  
  // 9칸
  { shape: [[1,1,1],[1,1,1],[1,1,1]], color: 6, name: 'square3' },
];
```

### 핵심 로직
```javascript
function canPlace(piece, gridX, gridY) {
  for (let py = 0; py < piece.shape.length; py++) {
    for (let px = 0; px < piece.shape[py].length; px++) {
      if (piece.shape[py][px]) {
        const gx = gridX + px;
        const gy = gridY + py;
        if (gx < 0 || gx >= GRID_SIZE || gy < 0 || gy >= GRID_SIZE) return false;
        if (grid[gy][gx] !== 0) return false;
      }
    }
  }
  return true;
}

function placePiece(piece, gridX, gridY) {
  for (let py = 0; py < piece.shape.length; py++) {
    for (let px = 0; px < piece.shape[py].length; px++) {
      if (piece.shape[py][px]) {
        grid[gridY + py][gridX + px] = piece.color;
      }
    }
  }
}

function checkAndClearLines() {
  let cleared = 0;
  const rowsToClear = [];
  const colsToClear = [];
  
  // 가로 줄 체크
  for (let y = 0; y < GRID_SIZE; y++) {
    if (grid[y].every(cell => cell !== 0)) rowsToClear.push(y);
  }
  
  // 세로 줄 체크
  for (let x = 0; x < GRID_SIZE; x++) {
    if (grid.every(row => row[x] !== 0)) colsToClear.push(x);
  }
  
  cleared = rowsToClear.length + colsToClear.length;
  
  // 클리어 실행
  rowsToClear.forEach(y => grid[y].fill(0));
  colsToClear.forEach(x => grid.forEach(row => row[x] = 0));
  
  return cleared;
}

function calculateScore(linesCleared) {
  if (linesCleared === 0) { combo = 0; return 0; }
  combo++;
  // 기본: 줄당 10점 × 줄 수 보너스 × 콤보
  const lineBonus = [0, 10, 30, 60, 100, 150][Math.min(linesCleared, 5)];
  const comboMultiplier = 1 + (combo - 1) * 0.5;
  return Math.floor(lineBonus * comboMultiplier);
}
```

### 게임 오버 판정
```javascript
function isGameOver() {
  // 남은 피스 중 하나라도 배치 가능한지 확인
  return currentPieces.every(piece => {
    if (!piece) return true; // 이미 사용된 피스
    for (let y = 0; y < GRID_SIZE; y++) {
      for (let x = 0; x < GRID_SIZE; x++) {
        if (canPlace(piece, x, y)) return false;
      }
    }
    return true; // 어디에도 배치 불가
  });
}
```

---

## UI/UX 플로우

### 메인 화면
```
┌─────────────────────┐
│  🧩 Block Fit Zen   │
│                     │
│   ⭐ Best: 12,450   │
│                     │
│   [▶ Classic]       │
│   [⏱ Time Attack]   │
│   [🧩 Puzzle]       │
│   [📅 Daily]        │
│                     │
│   🔥 Streak: 5 days │
└─────────────────────┘
```

### 게임 화면
```
┌─────────────────────┐
│  Score: 2,340       │
│  Combo: x3 🔥       │
├─────────────────────┤
│  ┌─┬─┬─┬─┬─┬─┬─┬─┬─┬─┐
│  │ │ │█│ │ │ │ │ │ │ │
│  ├─┼─┼─┼─┼─┼─┼─┼─┼─┼─┤
│  │ │█│█│█│ │ │ │ │ │ │
│  ├─┼─┼─┼─┼─┼─┼─┼─┼─┼─┤
│  │ │ │█│ │ │ │█│█│ │ │
│  ├─┼─┼─┼─┼─┼─┼─┼─┼─┼─┤
│  │ │ │ │ │ │ │█│ │ │ │
│  ... (10×10)
│  └─┴─┴─┴─┴─┴─┴─┴─┴─┴─┘
│                     │
│  [🟫🟫]  [🟩🟩🟩]  [🟦] │
│  [🟫  ]             [🟦] │
│                     [🟦] │
│  ← 3개의 배치 가능 피스 → │
└─────────────────────┘
```

### 인터랙션
1. 하단 피스 영역에서 블록을 터치/클릭
2. 드래그하여 그리드 위로 이동
3. 유효한 위치면 하이라이트 표시 (초록 테두리)
4. 놓으면 배치 + 줄 체크
5. 줄 완성 시 → 클리어 애니메이션 (파티클 + 스코어 팝업)
6. 3개 모두 배치 → 새 3개 피스 생성
7. 배치 불가 → 게임 오버 + 결과 화면

### 비주얼 테마 (기본: Wood)
- 배경: 따뜻한 크림색 (#FDF6E3)
- 그리드: 연한 나무색 격자
- 블록: 6가지 우드 톤 (연갈색~진갈색)
- 클리어 이펙트: 나뭇잎 파티클
- 폰트: 둥근 산세리프

---

## 밸런스 파라미터

```javascript
const BALANCE = {
  // 그리드
  grid: {
    size: 10,
    cellSize: 36,           // 모바일 최적 크기
  },

  // 피스 생성
  pieces: {
    perRound: 3,            // 한 번에 3개
    weightSmall: 40,        // 1-3칸 피스 확률 (%)
    weightMedium: 45,       // 4칸 피스 확률 (%)
    weightLarge: 15,        // 5-9칸 피스 확률 (%)
  },

  // 점수
  scoring: {
    placementBase: 1,       // 블록 1칸당 기본 점수
    lineClear: [0, 10, 30, 60, 100, 150],  // 1~5줄 동시 클리어
    comboMultiplier: 0.5,   // 콤보당 추가 배율
  },

  // 타임어택 모드
  timeAttack: {
    duration: 120,          // 2분
    bonusPerLine: 5,        // 줄 클리어당 +5초
  },

  // 일일 챌린지
  daily: {
    fixedSeed: true,        // 같은 날 같은 피스 순서
    rewardsPerStreak: [10, 20, 50, 100, 200], // 연속일 보너스
  },

  // 색상 팔레트 (우드 톤)
  colors: {
    wood: ['#D4A574', '#C49A6C', '#B08968', '#A07855', '#8B6914', '#6B4226'],
    neon: ['#FF6B6B', '#4ECDC4', '#45B7D1', '#96CEB4', '#FECA57', '#FF9FF3'],
    pastel: ['#FFB5B5', '#B5DEFF', '#B5FFB5', '#FFE4B5', '#E4B5FF', '#FFB5E4'],
  },

  // 광고
  ads: {
    interstitialInterval: 3,    // N게임마다 전면 광고
    rewardedExtraPiece: true,   // 게임오버 시 추가 피스 (보상광고)
  },
};
```

---

## 구현 우선순위

### MVP (핵심, 1차)
1. 10×10 그리드 렌더링
2. 블록 피스 3개 표시 + 드래그 앤 드롭
3. 배치 유효성 체크 + 배치
4. 줄 클리어 (가로/세로)
5. 점수 계산 + 콤보
6. 게임 오버 판정
7. 하이스코어 저장 (localStorage)

### 폴리시 (2차)
8. 줄 클리어 애니메이션 (파티클)
9. 드래그 시 그리드 하이라이트
10. 콤보 팝업 이펙트
11. 통계 화면 (게임 수, 최고점수 등)
12. 우드 테마 비주얼 완성

### 확장 (3차)
13. 타임어택 모드
14. 퍼즐 모드 (정해진 피스로 그리드 완성)
15. 일일 챌린지 (seed 기반)
16. 추가 테마 (네온, 파스텔)
17. 텔레그램 Mini App 통합
18. 사운드 이펙트

---
*생성일: 2026-01-31*
*기반 트렌드: Woodoku (100M+ DL), Block Puzzle 장르, 젠 캐주얼 퍼즐 수요 증가*
*포트폴리오 갭: 블록핏/우독 장르 미보유 → 시장 수요 높은 장르 확보*
