# 032 — Tap Away Puzzle 🧊

> "Tap Away" 메카닉 — 2026 앱스토어 퍼즐 급상승 장르 (Tap Block Away 3D 등 다수 히트)

## 🎯 게임 컨셉
2D 블록 퍼즐. 화살표가 표시된 블록을 탭하면 화살표 방향으로 슬라이딩해서 보드 밖으로 나감.
올바른 순서로 탭하지 않으면 블록이 다른 블록에 막혀 이동 불가. **순서를 추론하는 논리 퍼즐**.
"Tap Away 3D"의 핵심 쾌감을 2D에서 HTML5로 구현.

## 🕹️ 핵심 메카닉
1. **탭 제거:** 블록을 탭 → 화살표 방향으로 슬라이딩 → 보드 밖 도달 시 제거
2. **방해 블록:** 경로에 다른 블록이 있으면 이동 불가 (빨간 피드백)
3. **순서 퍼즐:** 올바른 순서로 탭해야 전부 제거 가능
4. **방향 전환:** 일부 블록은 탭할 때마다 방향 90° 회전 (특수 블록)
5. **연쇄 이동:** 한 블록 제거 시 경로가 열려 다른 블록 이동 가능
6. **스타 시스템:** 이동 횟수 기반 별 1-3개 (최소 이동 = 3스타)

## 👥 타겟 유저
- 로직/공간 퍼즐 선호자
- Parking Jam, Traffic Escape 유저 (유사 메카닉)
- 캐주얼 퍼즐 유저 (조작 단순: 탭 1가지)
- 10-50대 전 연령 (직관적)

## 💎 차별점
1. **2D 경량화:** 3D 버전과 달리 저사양에서도 부드러운 60fps
2. **순수 로직:** 운 요소 0%, 순수 퍼즐 만족감
3. **원터치 조작:** 탭만으로 플레이, 최소 접근 장벽
4. **레벨 에디터(향후):** 유저 생성 퍼즐 공유 가능성
5. **릴렉싱 + 도전:** 시간 제한 없음, 자기 페이스로 사고

## 💰 수익 모델
- **보상형 광고:**
  - 힌트 (다음 탭할 블록 하이라이트)
  - 되돌리기 (마지막 이동 취소)
  - 3스타 실패 시 재도전 없이 별 업그레이드
- **IAP:**
  - 광고 제거: $2.99
  - 힌트 팩 (50개): $1.99
  - 테마 팩 (블록 스킨): $0.99
- **인터스티셜:** 5레벨마다 (비침습적)

## 🛠️ 기술 스택
- HTML5 Canvas + Vanilla JavaScript
- CSS3 트랜지션 (블록 이동 애니메이션)
- LocalStorage (레벨 진행)
- 모바일 터치 + 데스크탑 클릭

---

## 📐 시스템 설계

### 게임 오브젝트
```javascript
Block {
  id: number,
  row: number,           // 그리드 위치 (0-7)
  col: number,           // 그리드 위치 (0-7)
  direction: 'up'|'down'|'left'|'right',
  color: string,         // 시각적 구분
  type: 'normal'|'rotating'|'locked',
  // normal: 고정 방향, 탭하면 이동
  // rotating: 탭할 때마다 90° 회전 (이동하려면 경로 열려야)
  // locked: 특정 조건 충족 전 이동 불가 (고급 레벨)
}

Level {
  id: number,
  gridSize: { rows: number, cols: number }, // 5x5 ~ 8x8
  blocks: Block[],
  parMoves: number,      // 최적 이동 횟수 (3스타 기준)
  difficulty: 'easy'|'medium'|'hard'|'expert',
}

GameState {
  level: number,
  moves: number,
  history: Move[],       // 되돌리기용
  blocksRemaining: number,
  hintsUsed: number,
  stars: { [levelId]: number }, // 레벨별 획득 별
  totalStars: number,
}

Move {
  blockId: number,
  fromRow: number,
  fromCol: number,
  direction: string,
}
```

### 이동 로직
```javascript
function canMove(block, grid) {
  const { row, col, direction } = block;
  let r = row, c = col;
  const dr = { up: -1, down: 1, left: 0, right: 0 };
  const dc = { up: 0, down: 0, left: -1, right: 1 };
  
  // 화살표 방향으로 한 칸씩 체크
  r += dr[direction];
  c += dc[direction];
  
  while (r >= 0 && r < gridRows && c >= 0 && c < gridCols) {
    if (grid[r][c] !== null) return false; // 경로에 블록 있음
    r += dr[direction];
    c += dc[direction];
  }
  
  // 보드 밖 도달 = 이동 가능
  return true;
}
```

### 레벨 생성 (역방향 설계)
```
1. 빈 보드에서 시작
2. 블록을 보드 밖에서 안쪽으로 역방향 배치
3. 배치 순서의 역순 = 해법
4. 여러 유효 해법이 존재하도록 조정
5. parMoves = 최적 해법 이동 수
```

### 스타 판정
```javascript
function calculateStars(moves, parMoves) {
  if (moves <= parMoves) return 3;       // 최적 또는 그 이상
  if (moves <= parMoves + 2) return 2;   // 거의 최적
  return 1;                               // 클리어
}
```

## 🎨 UI/UX 플로우

### 메인 화면
```
┌─────────────────────────┐
│   🧊 TAP AWAY PUZZLE    │
│                         │
│   [▶ PLAY]              │
│   [📋 레벨 선택]         │
│                         │
│   ⭐ 156/300  📊 Lv.52  │
│   [⚙️ 설정]             │
└─────────────────────────┘
```

### 게임 화면 (6×6 예시)
```
┌─────────────────────────┐
│ Level 23  Moves: 3      │
│ ⭐⭐⭐ (par: 5)        │
├─────────────────────────┤
│                         │
│     [→] [↓]             │
│  [↑]       [←]          │
│     [→] [↓]    [←]      │
│        [↑]              │
│  [←]       [→]          │
│                         │
├─────────────────────────┤
│ [↩️ Undo] [💡 Hint]     │
│ [🔄 Reset]              │
└─────────────────────────┘
```

### 블록 제거 애니메이션
```
1. 탭 → 블록 하이라이트 (짧은 펄스)
2. 화살표 방향으로 가속 슬라이딩 (200ms)
3. 보드 밖에서 페이드 + 파티클
4. 남은 블록 수 업데이트
5. 마지막 블록 제거 시 축하 이펙트
```

### 이동 불가 피드백
```
탭 → 블록 빨갛게 깜빡임 (150ms)
방해 블록도 살짝 흔들림 (알려주기)
```

## ⚖️ 밸런스 파라미터
```javascript
const BALANCE = {
  // 보드 크기
  GRID_EASY: { rows: 5, cols: 5 },
  GRID_MEDIUM: { rows: 6, cols: 6 },
  GRID_HARD: { rows: 7, cols: 7 },
  GRID_EXPERT: { rows: 8, cols: 8 },
  
  // 블록 수
  BLOCKS_EASY: [4, 6],     // min, max
  BLOCKS_MEDIUM: [6, 10],
  BLOCKS_HARD: [10, 15],
  BLOCKS_EXPERT: [15, 22],
  
  // 특수 블록 비율
  ROTATING_RATIO: { easy: 0, medium: 0.1, hard: 0.2, expert: 0.3 },
  LOCKED_RATIO: { easy: 0, medium: 0, hard: 0.05, expert: 0.1 },
  
  // 힌트
  FREE_HINTS: 5,           // 시작 무료 힌트
  HINT_AD_REWARD: 2,
  
  // 되돌리기
  MAX_UNDO: 3,             // 레벨당 무료 되돌리기
  UNDO_AD_REWARD: 3,
  
  // 레벨 배치
  TOTAL_LEVELS: 200,       // 초기 레벨 수
  LEVELS_PER_DIFFICULTY: {
    easy: 50,    // 1-50
    medium: 50,  // 51-100
    hard: 50,    // 101-150
    expert: 50,  // 151-200
  },
};
```

## 📈 리텐션 요소
- **완벽주의 동기:** 전 레벨 3스타 도전
- **일일 퍼즐:** 매일 새로운 특수 레벨
- **별 수집:** 총 별 수로 새 테마/스킨 해금
- **진행 만족감:** 레벨 맵 + 구간별 보상
- **경쟁:** 일일 퍼즐 최소 이동 리더보드

## 📊 시장 근거
- **Tap Away 3D:** Google Play 퍼즐 상위 10, 급성장 중 (2026)
- **Parking Jam 3D:** 유사 "순서대로 제거" 메카닉으로 4억+ DL
- **Traffic Escape:** 우리 포트폴리오에도 있음 — 유사 메카닉 검증됨
- **순수 로직 퍼즐:** 광고 eCPM 프리미엄 ($15-25), 교육 카테고리 이중 노출
- **HTML5 장점:** 3D 앱들이 무거운 반면, 2D 경량 버전으로 차별화

## 구현 우선순위
1. 그리드 렌더링 + 블록 배치
2. 화살표 방향 표시
3. 탭 → 이동 가능 판정 + 슬라이딩 애니메이션
4. 블록 제거 + 레벨 클리어 판정
5. 되돌리기(Undo) 시스템
6. 레벨 데이터 (50개 기본, 난이도 순차)
7. 스타 시스템 + 레벨 선택 화면
8. 힌트 시스템
9. 특수 블록 (rotating, locked)
10. 사운드 + 폴리시

---
*Created: 2026-01-30 19:00 | Status: 미구현 | Trend: Tap Away 3D 급성장, Parking Jam 유사 메카닉 검증*
